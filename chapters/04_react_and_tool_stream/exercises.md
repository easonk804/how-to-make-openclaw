# Exercises - Chapter 04

## Basic
1. Add a new rule in v1 for `ImportError`.
2. Add one extra reasoning step in v2 before final answer.

## Intermediate
1. In v3, emit a `tool_end` event with execution duration.
2. Add a second action retry when first observation says `failed`.

## Challenge
Make v3 return both user-safe explanation and internal detailed trace.
