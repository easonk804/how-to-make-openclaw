OpenClaw session keys can be main, per-peer, or per-channel-peer.
Queue lanes serialize runs per session while allowing bounded global concurrency.
Sandbox policies block dangerous file paths and restricted tools.
Compaction summarizes old context and prunes low-value tool outputs.
